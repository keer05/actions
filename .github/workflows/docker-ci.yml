name: Docker CI with Caching

on:
  push:
    branches:
      - test-2

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-docker-${{ hashFiles('**/docker/Dockerfile') }}  # Updated path to Dockerfile
          restore-keys: |
            ${{ runner.os }}-docker-

      - name: Build Docker image
        run: |
          docker buildx build --file docker/Dockerfile --tag testkeer .  # Updated path to Dockerfile

  tag:
    needs: build 
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker
        uses: docker/setup-docker@v2

      - name: Docker Tag Image
        run: |
          docker tag testkeer newdockerimg:v100
